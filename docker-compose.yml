version: "3.9"
services:
  stock-dashboard-next:
    image: postgres
    restart: always
    container_name: stock-dashboard-next
    ports:
      - 5429:5432
    environment:
      POSTGRES_PASSWORD: example
      PGDATA: /data/postgres
    volumes:
      - postgres:/data/postgres

volumes:
  postgres:
